Вижу проблему на скриншотах! Эти дефолтные системные ползунки прокрутки (особенно горизонтальный под фильтрами) полностью убивают ощущение нативного мобильного приложения. Их нужно срочно скрыть через CSS, сохранив при этом саму возможность скроллить пальцем/мышкой. 

Ниже я подготовил для тебя следующий промпт. Скопируй его и отправь своему ИИ-кодеру (Cursor, Windsurf и т.д.). Он заставит его починить стили скролла и прописать код для трех недостающих экранов.

***

**Скопируй и отправь этот текст нейросети:**

```text
ЗАДАЧА:
1. КРИТИЧЕСКИЙ FIX UI: Убрать системные ползунки прокрутки (скроллбары) по всему приложению, особенно горизонтальный ползунок в фильтрах на Главном экране и вертикальный на экране Профиля. Приложение должно выглядеть как нативное мобильное, скролл должен работать, но сам ползунок должен быть невидим.
2. РЕАЛИЗОВАТЬ 3 ВЛОЖЕННЫХ ЭКРАНА ПРОФИЛЯ: «Избранное», «Адреса и карты», «Настройки».

---
ШАГ 1: ГЛОБАЛЬНЫЙ ФИКС СКРОЛЛБАРОВ
Добавь в глобальный файл стилей (например, `index.css` или `globals.css`) следующий утилитный класс для скрытия ползунков:
```css
@layer utilities {
  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
}
```
ОБЯЗАТЕЛЬНО примени класс `no-scrollbar` к:
- Контейнеру с горизонтальными фильтрами ("Все", "Деним" и т.д.) на главном экране.
- Главным оборачивающим контейнерам (Layout/Main), чтобы убрать вертикальную полосу прокрутки.

---
ШАГ 2: РЕАЛИЗАЦИЯ ЭКРАНОВ (Используй React, Tailwind CSS, Lucide React)

На всех трех экранах сверху слева должна быть кнопка "Назад" (ChevronLeft, цвет #556B2F), ведущая на `/profile`.

ЭКРАН 1: ИЗБРАННОЕ (`/favorites`)
- Заголовок: "Избранное"
- Контент: Сетка карточек (2 колонки, `grid-cols-2`, `gap-4`).
- Карточка: Квадратное фото. В правом верхнем углу поверх фото — круглая кнопка с залитым оливковым сердечком (Heart). Под фото название вещи, цена (жирным #556B2F) и кнопка "Хочу так же" (bg-[#556B2F], text-white, rounded-xl).
- Empty State (если пусто): Большая серая иконка разбитого сердца (Heart с opacity), текст "Вы пока ничего не добавили".

ЭКРАН 2: АДРЕСА И КАРТЫ (`/addresses-cards`)
- Заголовок: "Мои адреса и карты"
- Секция 1: "Адреса доставки". Список карточек адресов (иконка MapPin слева, адрес по центру, иконки Pencil/Trash справа). 
- Кнопка под адресами: Широкая кнопка "+ Добавить новый адрес" (пунктирная обводка `border-dashed`, цвет #556B2F).
- Секция 2: "Способы оплаты". Список привязанных карт (например, "Visa **** 4242", иконка крестика для удаления).
- Кнопка под картами: "+ Привязать карту" (пунктирная обводка).

ЭКРАН 3: НАСТРОЙКИ (`/settings`)
- Заголовок: "Настройки"
- Блок "УВЕДОМЛЕНИЯ" (серый заголовок): 3 строки (Push, Email, SMS). Справа от каждой — Toggle Switch (переключатель). Если включен — оливковый, выключен — серый.
- Блок "ВНЕШНИЙ ВИД": Строка "Темная тема" + Toggle Switch.
- Блок "ОБЩИЕ": Список со стрелочками вправо (ChevronRight): Язык (с припиской "Русский"), Политика конфиденциальности, Оценить приложение.
- В самом низу (Опасная зона):
  - Кнопка "Выйти из аккаунта" (Текст #757575, font-medium, по центру)
  - Кнопка "Удалить аккаунт" (Текст text-red-600, font-medium, по центру)

Напиши полные компоненты для этих страниц и убедись, что роуты прописаны!
```
Держи. Я выжал максимум из обоих вариантов, убрал воду и собрал ультимативный инженерный Master-Prompt. Это не просто текст, это жесткая архитектурная инструкция для нейросети (Cursor, Claude 3.5 Sonnet, v0 или Windsurf), которая заставит ее написать чистый, красивый и полностью рабочий код на React. 

В нем объединена сложная маршрутизация (B2C/B2B), эстетика Figma (Pinterest-сетка, скругления, тени) и логика состояний. Добавил требование использовать `framer-motion`, чтобы интерфейс не просто работал, а плавно и дорого анимировался.

Просто копируй блок ниже и скармливай ИИ-кодеру:

***

# УЛЬТИМАТИВНЫЙ MASTER-PROMPT: РАЗРАБОТКА FRONTEND АРХИТЕКТУРЫ "APPCYCLING"

**РОЛЬ ИИ:** Ты — Senior Frontend Developer и Lead UI/UX Designer. Твоя задача — написать production-ready код для мобильного веб-приложения "Appcycling" (экологичная переработка и ИИ-апсайклинг одежды). Код должен быть модульным, чистым и невероятно красивым.

**ТЕХНИЧЕСКИЙ СТЕК:** 
React (Functional Components, Hooks), Tailwind CSS (для стилизации), React Router DOM (Data mode, `createBrowserRouter`), Lucide React (для всех иконок), Framer Motion (для плавных микроинтеракций и переходов). Валюта в приложении: `₸` (KZT). Язык: Строгий русский.

## 1. ГЛОБАЛЬНАЯ ДИЗАЙН-СИСТЕМА (СТРОГОЕ СОБЛЮДЕНИЕ)
*   **Цвета:** Акцентный: Оливковый `#556B2F` (кнопки, активные иконки, прогресс-бары). Фон: Чисто белый `#FFFFFF` (карточки) и Светло-серый `#F5F5F5` (фон экранов). Текст: Угольный `#212121` (заголовки), Серый `#757575` (вторичный текст), Ошибки/Удаление: `#EF4444`.
*   **Типографика:** Шрифт `Inter` на всех экранах.
*   **Формы и Тени:** Скругления `16px` (`rounded-2xl`) для кнопок, модалок, полей. Скругления `12px` для изображений. Тени: мягкие и дорогие `box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.08)`.
*   **Глобальная навигация (Top Bar):** На всех вложенных экранах (кроме корневых TabBar) слева сверху ОБЯЗАТЕЛЬНА кнопка "Назад" (иконка `ChevronLeft`, цвет `#556B2F`).
*   **Глобальная навигация (Bottom TabBar):** Присутствует на корневых B2C экранах. 
    *   Slot 1: "Главная" (`Home`). 
    *   Slot 2: "Заказы" (`Package`). 
    *   **Slot 3 (ЦЕНТР):** Огромная плавающая кнопка (FAB), перекрывающая TabBar. Идеальный круг, сплошная заливка `#556B2F`, иконка `Sparkles` (белая). Ведет на ИИ-генератор.
    *   Slot 4: "Корзина" (`ShoppingCart`). 
    *   Slot 5: "Профиль" (`User`).

## 2. МАРШРУТИЗАЦИЯ И СТРУКТУРА (React Router)
Приложение разделено на 2 изолированных флоу: B2C (физлица) и B2B (бизнес).
*   **Root:** `/` (Welcome)
*   **B2C Auth:** `/b2c-auth`, `/login`, `/register`
*   **B2C Main:** `/home` (Каталог), `/ai-camera`, `/ai-results`, `/constructor` (ИИ-редактор), `/cart`, `/orders`, `/notifications`
*   **B2C Checkout:** `/delivery`, `/payment`, `/status`
*   **B2C Profile:** `/profile`, `/eco-impact`, `/favorites`, `/addresses-cards`, `/settings`
*   **B2B Flow:** `/b2b-login`, `/b2b-portal`

## 3. ДЕТАЛИЗАЦИЯ ЭКРАНОВ И UI-СОСТОЯНИЙ (РЕНДЕРИТЬ С ВЫСОКОЙ ТОЧНОСТЬЮ)

### БЛОК А: ОНБОРДИНГ И ВЫБОР РОЛИ
*   **`/` (Welcome):** Полноэкранное фото апсайкл-одежды, логотип. Две кнопки: "Я физическое лицо" (bg-`#556B2F`, ведет на `/b2c-auth`) и "Я представляю компанию (B2B)" (Outline, border-`#556B2F`, ведет на `/b2b-login`).
*   **`/b2c-auth` -> `/login` -> `/register`:** Минималистичные формы входа. Иконки в полях (`User`, `Mail`, `Lock`). Плавный переход между входом и регистрацией через текстовые ссылки внизу.

### БЛОК Б: B2C ГЛАВНАЯ (HOME / CATALOG)
*   **Шапка:** Логотип слева. Справа иконка `Bell` (Колокольчик). Если есть уведомления — красная точка-индикатор.
*   **Поиск и Фильтры:** Поле поиска + горизонтальный скролл фильтров: "Все" (Активный: bg-`#556B2F`), "Деним", "Худи" (Неактивные: border-grey).
*   **Контент (Masonry Grid):** Сетка в стиле Pinterest. Качественные фото. Внутри каждой карточки: в правом верхнем углу круглая белая кнопка с иконкой `Heart` (Избранное). Под фото: кнопка "Хочу так же" (bg-`#556B2F`).

### БЛОК В: ИИ-ЭКОСИСТЕМА (CONSTRUCTOR)
*   **`/constructor`:** Открывается после генерации ИИ. 
    *   **Центр:** Крупное реалистичное превью вещи (aspect-ratio 4:3).
    *   **Bottom Sheet (Тулкит):** Всплывающая белая панель снизу. Горизонтальный список круглых кнопок: "Цвет" (`Palette`), "Фурнитура" (`Paperclip`), "Текст" (`Type`). Кнопка сброса: "Отменить изменения" (`RotateCcw`, текст `#757575`).
    *   **Sticky Footer:** Приклеена к низу огромная кнопка "Готово к заказу - 15 000 ₸" (bg-`#556B2F`, 100% width, rounded-2xl). Ведет на `/delivery`.

### БЛОК Г: ОФОРМЛЕНИЕ ЗАКАЗА (CHECKOUT)
*   **`/delivery`:** Интерактивные состояния карточек доставки.
    *   *Курьер (Активное состояние):* Толстая обводка `#556B2F`, зеленая галочка (`CheckCircle`), иконка `Truck`.
    *   *Самовывоз (Неактивное):* Серая обводка `#E0E0E0`, нет галочки, иконка `Store`.
*   **Блок "Итого":** Крупно "Итоговая сумма: 15 000 ₸" (font-bold, text-`#556B2F`). Липкая кнопка "Оплатить и подтвердить".

### БЛОК Д: УВЕДОМЛЕНИЯ И ПРОФИЛЬ
*   **`/notifications`:** 
    *   *Непрочитанное:* Фон карточки `#556B2F` с opacity 5%. Оливковая точка слева. Жирный шрифт. (Например: "Ваши эскизы готовы! ИИ сгенерировал 5 вариантов").
    *   *Прочитанное:* Чисто белый фон, нет точки, серый текст времени.
*   **`/eco-impact` (Геймификация):** 
    *   Градиентный баннер (светло-зеленый к белому). Крупные счетчики: "12.5 кг" (текстиля спасено) и "4500 л" (воды сэкономлено).
    *   Прогресс-бар "8 из 10 заказов" (80% заполнено `#556B2F`). Кнопка `Share` для виральности.

### БЛОК Е: КОРПОРАТИВНЫЙ ХАБ (B2B PORTAL)
*   **`/b2b-portal`:** Строгий дизайн, НЕТ нижнего TabBar.
*   Вертикальный список из 3 массивных карточек: "Корпоративный мерч" (`Shirt`), "Заказ оптом" (`Briefcase`), "Продажа материалов" (`Recycle`).
*   При клике открывается BottomSheet форма лидогенерации (ИНН, Тираж, Имя, Телефон). После отправки — Success Screen с кнопками `Telegram`, `Mail`, `Phone`.

**ТРЕБОВАНИЯ К КОДУ:**
1. Выдели TabBar, Layout, Button, Input в переиспользуемые UI-компоненты.
2. Используй `AnimatePresence` и `motion.div` из framer-motion для переходов между экранами и появления BottomSheet.
3. Код должен быть готов к копипасту: используй заглушки (mock data) для изображений через Unsplash (например: `[https://images.unsplash.com/photo-XXX](https://images.unsplash.com/photo-XXX)`) и моковые стейты для функционала. 
4. Начни с генерации базовой структуры маршрутизатора и Layout компонентов.
Вижу проблему на скриншотах! Эти дефолтные системные ползунки прокрутки (особенно горизонтальный под фильтрами) полностью убивают ощущение нативного мобильного приложения. Их нужно срочно скрыть через CSS, сохранив при этом саму возможность скроллить пальцем/мышкой. 

Ниже я подготовил для тебя следующий промпт. Скопируй его и отправь своему ИИ-кодеру (Cursor, Windsurf и т.д.). Он заставит его починить стили скролла и прописать код для трех недостающих экранов.

***

**Скопируй и отправь этот текст нейросети:**

```text
ЗАДАЧА:
1. КРИТИЧЕСКИЙ FIX UI: Убрать системные ползунки прокрутки (скроллбары) по всему приложению, особенно горизонтальный ползунок в фильтрах на Главном экране и вертикальный на экране Профиля. Приложение должно выглядеть как нативное мобильное, скролл должен работать, но сам ползунок должен быть невидим.
2. РЕАЛИЗОВАТЬ 3 ВЛОЖЕННЫХ ЭКРАНА ПРОФИЛЯ: «Избранное», «Адреса и карты», «Настройки».

---
ШАГ 1: ГЛОБАЛЬНЫЙ ФИКС СКРОЛЛБАРОВ
Добавь в глобальный файл стилей (например, `index.css` или `globals.css`) следующий утилитный класс для скрытия ползунков:
```css
@layer utilities {
  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
}
```
ОБЯЗАТЕЛЬНО примени класс `no-scrollbar` к:
- Контейнеру с горизонтальными фильтрами ("Все", "Деним" и т.д.) на главном экране.
- Главным оборачивающим контейнерам (Layout/Main), чтобы убрать вертикальную полосу прокрутки.

---
ШАГ 2: РЕАЛИЗАЦИЯ ЭКРАНОВ (Используй React, Tailwind CSS, Lucide React)

На всех трех экранах сверху слева должна быть кнопка "Назад" (ChevronLeft, цвет #556B2F), ведущая на `/profile`.

ЭКРАН 1: ИЗБРАННОЕ (`/favorites`)
- Заголовок: "Избранное"
- Контент: Сетка карточек (2 колонки, `grid-cols-2`, `gap-4`).
- Карточка: Квадратное фото. В правом верхнем углу поверх фото — круглая кнопка с залитым оливковым сердечком (Heart). Под фото название вещи, цена (жирным #556B2F) и кнопка "Хочу так же" (bg-[#556B2F], text-white, rounded-xl).
- Empty State (если пусто): Большая серая иконка разбитого сердца (Heart с opacity), текст "Вы пока ничего не добавили".

ЭКРАН 2: АДРЕСА И КАРТЫ (`/addresses-cards`)
- Заголовок: "Мои адреса и карты"
- Секция 1: "Адреса доставки". Список карточек адресов (иконка MapPin слева, адрес по центру, иконки Pencil/Trash справа). 
- Кнопка под адресами: Широкая кнопка "+ Добавить новый адрес" (пунктирная обводка `border-dashed`, цвет #556B2F).
- Секция 2: "Способы оплаты". Список привязанных карт (например, "Visa **** 4242", иконка крестика для удаления).
- Кнопка под картами: "+ Привязать карту" (пунктирная обводка).

ЭКРАН 3: НАСТРОЙКИ (`/settings`)
- Заголовок: "Настройки"
- Блок "УВЕДОМЛЕНИЯ" (серый заголовок): 3 строки (Push, Email, SMS). Справа от каждой — Toggle Switch (переключатель). Если включен — оливковый, выключен — серый.
- Блок "ВНЕШНИЙ ВИД": Строка "Темная тема" + Toggle Switch.
- Блок "ОБЩИЕ": Список со стрелочками вправо (ChevronRight): Язык (с припиской "Русский"), Политика конфиденциальности, Оценить приложение.
- В самом низу (Опасная зона):
  - Кнопка "Выйти из аккаунта" (Текст #757575, font-medium, по центру)
  - Кнопка "Удалить аккаунт" (Текст text-red-600, font-medium, по центру)

Напиши полные компоненты для этих страниц и убедись, что роуты прописаны!
```
ЗАДАЧА:
1. КРИТИЧЕСКИЙ FIX UI: Скрыть системные ползунки прокрутки (скроллбары) по всему приложению, особенно горизонтальный ползунок в фильтрах на Главном экране. Скролл должен работать, но сам ползунок должен быть невидим.
2. Сверстать 3 экрана профиля («Избранное», «Адреса и карты», «Настройки») в точном соответствии с предоставленными референсами (image_a4e09a.png и image_a4e3ba.png).

---
ШАГ 1: ГЛОБАЛЬНЫЙ ФИКС СКРОЛЛБАРОВ
Добавь в глобальный файл стилей (`index.css` или `globals.css`) следующий утилитный класс:

@layer utilities {
  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
}

ОБЯЗАТЕЛЬНО примени класс `no-scrollbar` к контейнеру с горизонтальными фильтрами ("Все", "Деним", "Худи" и т.д.) на главном экране.

---
ШАГ 2: РЕАЛИЗАЦИЯ ЭКРАНОВ (React, Tailwind CSS, Lucide React)
На всех экранах сверху слева должна быть кнопка "Назад" (ChevronLeft, цвет #556B2F), ведущая на `/profile`. Цвет фона экранов: светло-серый `#F5F5F5`.

ЭКРАН 1: ИЗБРАННОЕ (`/favorites`) - Строго по референсу image_a4e09a.png
- Заголовок: крупный, "Избранное" (угольный цвет #212121, font-bold).
- Контент: Сетка карточек (2 колонки, `grid-cols-2`, `gap-4`).
- Карточка товара:
  - Белый фон (`bg-white`), скругление `rounded-2xl`, мягкая тень.
  - Фотография (aspect-ratio примерно 4:5).
  - Поверх фото в правом верхнем углу: круглая белая кнопка. Внутри неё ЗАЛИТОЕ оливковое сердце (иконка Heart, `fill="#556B2F" text-[#556B2F]`).
  - Текстовый блок под фото: Название ("Деним Куртка", "Худи Апсайкл" - цвет #212121), под ним Цена ("12 500 ₸", "15 000 ₸" - жирным, оливковым #556B2F).
  - Кнопка в самом низу карточки: "Хочу так же" (Сплошная оливковая `bg-[#556B2F]`, белый текст, `rounded-xl`, на всю ширину карточки).

ЭКРАН 2: АДРЕСА И КАРТЫ (`/addresses-cards`) - Строго по референсу image_a4e3ba.png
- Секция 1: Заголовок "АДРЕСА ДОСТАВКИ" (uppercase, font-semibold, серый цвет #757575).
  - Карточка адреса: Белый фон, `rounded-2xl`. Слева круглая иконка MapPin на светло-оливковом фоне. По центру: "г. Алматы, ул. Абая 14" (font-bold) и ниже "Кв. 42, Подъезд 2" (серый текст). Справа иконки Pencil (редактировать) и Trash (удалить).
  - Кнопка добавления: Под карточкой широкая кнопка "+ Добавить новый адрес". Оливковый текст, пунктирная оливковая обводка (`border-dashed border-[#556B2F]`, `rounded-2xl`).
- Секция 2: Заголовок "СПОСОБЫ ОПЛАТЫ" (uppercase, font-semibold, серый цвет #757575).
  - Карточка карты: Белый фон. Слева синий прямоугольник с логотипом VISA. По центру "Visa **** 4242" (font-bold) и ниже "05/25" (серый текст). Справа иконка X (крестик для удаления).
  - Кнопка добавления: Под карточкой широкая кнопка "+ Привязать карту". Оливковый текст, сплошная темная обводка (`border-solid border-[#212121]`, `rounded-2xl`).

ЭКРАН 3: НАСТРОЙКИ (`/settings`)
- Заголовок: "Настройки"
- Блок "УВЕДОМЛЕНИЯ": 3 строки (Push-уведомления, Email-уведомления, SMS-уведомления). Справа от каждой — Toggle Switch. Если включен — ползунок оливковый `bg-[#556B2F]`, выключен — серый `bg-[#E0E0E0]`.
- Блок "ОБЩИЕ": Список (Язык приложения, Политика конфиденциальности, Оценить приложение) с иконкой `ChevronRight` справа.
- Опасная зона (внизу экрана): 
  - Кнопка "Выйти из аккаунта" (Текст серый `#757575`, по центру).
  - Кнопка "Удалить аккаунт" (Текст красный `text-red-600`, по центру).
ЗАДАЧА: 
Оживить экран «Мои адреса и карты» (/addresses-cards). 
Требуется реализовать полноценный функционал добавления и удаления адресов и банковских карт с использованием локального состояния (useState) и всплывающих модальных окон (Bottom Sheet/Modal) для форм добавления.

ШАГ 1: ИНИЦИАЛИЗАЦИЯ СТЕЙТОВ И MOCK-ДАННЫХ
Создай внутри компонента следующие состояния:
1. `addresses`: массив объектов (по умолчанию один элемент: `г. Алматы, ул. Абая 14`, `Кв. 42, Подъезд 2`).
2. `cards`: массив объектов (по умолчанию: `Visa **** 4242`, `05/25`).
3. Стейты для управления видимостью модальных окон: `isAddressModalOpen`, `isCardModalOpen`.

ШАГ 2: ФУНКЦИОНАЛ УДАЛЕНИЯ
- На карточке адреса кнопка с иконкой `Trash` должна вызывать функцию `deleteAddress(id)`, которая отфильтрует массив `addresses`.
- На карточке оплаты кнопка с иконкой `X` должна вызывать функцию `deleteCard(id)`, отфильтровывающую массив `cards`.

ШАГ 3: ФОРМЫ ДОБАВЛЕНИЯ (МОДАЛЬНЫЕ ОКНА)
Реализуй два модальных окна (полупрозрачный черный фон `bg-black/50`, поверх него белая плашка `bg-white` со скруглениями `rounded-t-3xl` или `rounded-3xl`, прижатая к низу или по центру).

МОДАЛКА 1: Добавление адреса (Открывается по кнопке "+ Добавить новый адрес")
- Заголовок: "Новый адрес" (font-bold, text-xl).
- Поля ввода (input): 
  1. "Город, улица" (placeholder: "Например: Абая 14")
  2. "Квартира / Подъезд" (placeholder: "Например: Кв. 42, Подъезд 2")
- Кнопка: "Сохранить адрес" (Сплошная, bg-[#556B2F], text-white, w-full, rounded-2xl, py-4). При клике добавляет новый объект в стейт `addresses` и закрывает модалку.

МОДАЛКА 2: Привязка карты (Открывается по кнопке "+ Привязать карту")
- Заголовок: "Новая карта" (font-bold, text-xl).
- Поля ввода:
  1. "Номер карты" (placeholder: "0000 0000 0000 0000")
  2. В один ряд (grid-cols-2 gap-4): "Срок действия" (MM/YY) и "CVC".
- Кнопка: "Привязать" (Сплошная, bg-[#556B2F], text-white, w-full, rounded-2xl, py-4). При клике добавляет карту в стейт `cards` и закрывает модалку.

ШАГ 4: EMPTY STATES (ЕСЛИ УДАЛИЛИ ВСЕ)
- Если `addresses.length === 0`, вместо списка показывай серый текст "Нет сохраненных адресов".
- Если `cards.length === 0`, показывай серый текст "Нет привязанных карт".

ТРЕБОВАНИЯ К КОДУ:
Стилизуй все инпуты (input) внутри модалок: `border border-[#E0E0E0]`, `rounded-2xl`, `px-4`, `py-3`, фокус должен менять цвет обводки на оливковый `focus:border-[#556B2F] outline-none`.
Используй AnimatePresence из framer-motion (если установлен) для плавного появления/исчезновения модалок и элементов списка при удалении.
ЗАДАЧА:
Реализовать глобальную систему "Избранного" (Favorites), которая синхронизирует состояние между Главным экраном (`/home` или `/catalog`) и экраном Избранного (`/favorites`).

ШАГ 1: ГЛОБАЛЬНЫЙ СТЕЙТ (React Context или Zustand)
Создай глобальное хранилище для избранных товаров.
1. Стейт должен хранить массив объектов `favorites` (свойства: id, title, price, image).
2. Создай функцию `toggleFavorite(item)`:
   - Если `item.id` уже есть в массиве `favorites` -> удалить его из массива (unfavorite).
   - Если `item.id` нет в массиве -> добавить его в массив (favorite).
3. Создай функцию `isFavorite(id)` -> возвращает `true`, если товар в избранном.

ШАГ 2: ИНТЕГРАЦИЯ НА ГЛАВНОМ ЭКРАНЕ (Лента товаров)
Обнови карточки товаров в сетке на главной странице:
- Круглая белая кнопка в правом верхнем углу фото теперь должна быть кликабельной.
- Привяжи к ней `onClick={() => toggleFavorite(item)}`.
- Динамическая иконка `Heart` (Lucide React):
  - Если `isFavorite(item.id) === true`: Иконка ЗАЛИТАЯ оливковым цветом (`fill="#556B2F" color="#556B2F"`).
  - Если `isFavorite(item.id) === false`: Иконка ПУСТАЯ (outline), цвет темно-серый (`color="#212121"`).

ШАГ 3: ИНТЕГРАЦИЯ НА ЭКРАНЕ "ИЗБРАННОЕ" (`/favorites`)
Обнови экран `/favorites`:
- Вместо моковых данных, рендери список карточек из глобального стейта `favorites`.
- Кнопка с сердечком здесь всегда ЗАЛИТАЯ оливковым (так как это уже избранное).
- При клике на залитое сердечко вызывается `toggleFavorite(item)`, что моментально удаляет карточку из списка и обновляет UI.
- Анимация: Оберни список в `AnimatePresence` (Framer Motion), чтобы при удалении карточка плавно исчезала (opacity: 0, scale: 0.9).
- EMPTY STATE: Если массив `favorites` пуст (`favorites.length === 0`), показывай заглушку: серая иконка разбитого сердца (Heart, opacity-30, большая) и текст "Вы пока ничего не добавили".

ТРЕБОВАНИЯ К UI (Важно для карточек):
- Кнопка сердечка: `absolute top-3 right-3 w-8 h-8 bg-white rounded-full flex items-center justify-center shadow-md cursor-pointer transition-transform active:scale-95`.
- Все карточки должны сохранять консистентный дизайн: скругления `rounded-2xl`, цена жирным оливковым шрифтом `text-[#556B2F]`, нижняя кнопка "Хочу так же" `bg-[#556B2F] text-white w-full rounded-xl py-2`.
